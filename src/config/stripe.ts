// src/config/stripe.ts
// import { loadStripe } from '@stripe/stripe-js';

// Commenting out the stripePromise
// const stripePromise = loadStripe('your-publishable-key-here');

// export default stripePromise;
